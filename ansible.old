[defaults]
pipelining = True
timeout = 30
forks = 400
strategy = free

[privilege_escalation]
become_user = ansible
become_method = sudo

[ssh_connection]
scp_if_ssh = True
retries = 5
ssh_args = -o ControlMaster=auto -o ControlPersist=60s
